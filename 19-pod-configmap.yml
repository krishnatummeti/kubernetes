apiVersion: v1
kind: Pod
metadata: 
  name: nginx-configmap
spec:
  container:
    - name: nginx-configmap
      image: nginx:latest
      envfrom:
        - configMapRef:
          name: my-configmap
