apiVersion: v1
kind: Pod
metadata:
  name: nginx-annotated
  annotations:
    purpose: "demo"
    owner: "techbasehub"
    env: "dev"
  spec:
    containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
      - containerPort: 80 

