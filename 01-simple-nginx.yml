# This is simple nginx pod yaml file
apiVersion: v1
kind: Pod
metadata:
  name: Nginx-App-Pod
  labels:
    env: Dev
    type: frontend
spec:
  containers: 
  - name: Nginx-App-container
    image: nginx:latest
    ports:
    - containerPort: 80