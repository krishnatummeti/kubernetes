# A simple example of ClusterConfig object:
---
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: cluster-1
  region: us-east-1

managedNodeGroups:
  - name: test-01
    instanceType: t3.micro
    desiredCapacity: 1
