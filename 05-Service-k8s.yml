# This file is Service yaml file

apiVersion: v1
kind: Service
metadata:
  name: nginx-Service
  labels:
    env: Dev
    type: frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
  selector:
    env: Dev